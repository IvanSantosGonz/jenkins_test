pipeline {
    agent {
        node {
            label 'built-in'
        }
    }
    stages {
          stage('test') {
            steps {

                sh "echo 'pepe'"
            }
          }
    }
}
